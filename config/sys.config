[
 {kernel,
  [{logger, [{handler, default, logger_std_h,
              #{formatter => {jt_log_formatter,
                              #{format => text, color => true}}}
             }]},
   {logger_level, info}
  ]}
].
